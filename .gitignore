build/
sim/build/
tools/qspi_loader/build/
.vscode/.cmake/
Debug/
Release/
.metadata/
*.o
*.d
*.elf
*.map

# EEZ Studio
# Generated code goes to Core/User/ui. This is the leftover output from before
# the destination folder was changed and does not belong in version control.
dashboard_layout/src/
# Editor view state (zoom, selection, scroll position) - differs per person.
*.eez-project-ui-state

# GIF assets
# Sources are the masters and are kept; the optimised output is derived from
# them by tools/gif_optimize.py and can be regenerated at any time.
dashboard_layout/gif/optimized/
# Built from the optimised GIFs by tools/make_qspi_image.py
dashboard_layout/gif/qspi.bin
